int multiply(int arr[], int n)
{
    // Complete the function
    int left=0,right=0,i;
    int mid=n/2;
    for(i=0;i<mid;i++)
    {
        left+=arr[i];
    }
    for(i=mid;i<n;i++)
    {
        right+=arr[i];
    }
    return left*right;
}
